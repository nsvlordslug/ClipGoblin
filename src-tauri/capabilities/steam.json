{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "steam-default",
  "description": "Steam desktop permissions without the standalone updater",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message",
    "dialog:allow-ask",
    "opener:allow-default-urls",
    "process:default",
    "process:allow-restart"
  ]
}
